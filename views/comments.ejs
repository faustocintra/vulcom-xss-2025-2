<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comentários</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            background-color: #f4f4f4;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 50%;
        }
        input, textarea {
            display: block;
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
        .comments {
            margin-top: 20px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Deixe um comentário</h2>
        <form action="/comment" method="POST">
            <textarea name="content" required></textarea>
            <button type="submit">Enviar</button>
        </form>
        <div class="comments">
            <h3>Comentários:</h3>
            <% comments.forEach(comment => { %>
                <!-- 
                  A linha vulnerável foi substituída.
                  <p><%- comment.content %></p>  <-- VULNERÁVEL 🚨: Renderiza HTML e executa scripts.
                -->
                <p><%= comment.content %></p> <!-- CORRIGIDO ✅: Escapa o HTML, tratando-o como texto. -->
            <% }); %>
        </div>
    </div>
</body>
</html>

<!-- O front-end era o ponto onde a vulnerabilidade de XSS se manifestava, 
executando no navegador do usuário o conteúdo malicioso que foi salvo no banco de dados.

A tag EJS usada para exibir o conteúdo dos comentários foi trocada de <%- comment.content %> para <%= comment.content %>.

Vulnerabilidade Corrigida: Stored Cross-Site Scripting (XSS). A tag <%- %> interpreta e renderiza qualquer HTML ou 
tag script presente no conteúdo do comentário. Isso permitia que um invasor salvasse um script no banco de dados, 
que seria executado no navegador de todos os usuários que visualizassem a página.

Impacto da Correção: A tag <%= %> realiza o escape de caracteres especiais de HTML 
(por exemplo, transformando < em &lt; e > em &gt;). Como resultado, qualquer script malicioso é tratado 
como texto simples e exibido de forma inofensiva na tela, em vez de ser executado pelo navegador. 
Esta alteração neutraliza diretamente a vulnerabilidade de XSS, que era a falha principal da aplicação. -->