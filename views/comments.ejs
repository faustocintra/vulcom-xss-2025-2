<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comentários</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            background-color: #f4f4f4;
        }
        .container {
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 50%;
        }
        input, textarea {
            display: block;
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
        .comments {
            margin-top: 20px;
            text-align: left;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Deixe um comentário</h2>
        <form action="/comment" method="POST">
            <!-- CORREÇÃO FRONTEND: Limitação no próprio HTML -->
            <!-- maxlength="500": Impede entrada de textos muito longos -->
            <!-- placeholder: Informa ao usuário sobre a limitação -->
            <textarea name="content" required maxlength="500" placeholder="Digite seu comentário (máximo 500 caracteres)"></textarea>
            <button type="submit">Enviar</button>
        </form>
        <div class="comments">
            <h3>Comentários:</h3>
            <% comments.forEach(comment => { %>
                <!-- CORREÇÃO CRÍTICA: Mudança de <%- para <%= -->
                <!-- <%- comment.content %>: VULNERÁVEL - Renderiza HTML sem escape -->
                <!-- <%= comment.content %>: SEGURO - Faz escape automático de HTML -->
                <!-- Exemplo: <script> vira &lt;script&gt; (texto visível, não executável) -->
                <p><%= comment.content %></p> <!-- CORRIGIDO: Agora usa escape automático -->
            <% }); %>
        </div>
    </div>
</body>
</html>
